// This is the template used to create an
//
// appsettings.{env}.INSECURE.json file  specific 
// to an Environment.
// It is NOT checked in (for security reasons), and 
// the reasons that devs that check out the code for 
// the first time have to recreate it from 
// this template file.
//
// IMPORTANT:
// filled and empty values ("") in appsettings.{env}.INSECURE.json
// override values already read from appsettings.{env}.json
// If you do not want to override an existing value, 
// comment it out here.
{


  "app": {
    "modules": {
      "core": {
        // -=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=
        "AzureEnvironment": {
          // TODO: in an *.INSECURE.json (obviously not a KV...), provide the name of the KV within the above ResourceGroup.
          // The KV used to retrieve Configuration variables (see Program.cs).
          // May not necessarily be in same resource group as app service.
          "DefaultKeyVaultResourceName": "",
          // -------------------------
          // TODO: In the AzureEnvironment defined KeyVault (or appSettings.Development.INSECURE.json) provide Azure SubscriptionId.
          "SubscriptionId": "",
          // TODO: In the AzureEnvironment defined KeyVault (or appSettings.Development.INSECURE.json) provide the Id of the AAD tenant managing the above subscription.
          "TenantId": "",
          // TODO: In the AzureEnvironment defined KeyVault (or appSettings.Development.INSECURE.json) provide the target environment ResourceGroup.
          "ResourceGroupName": "",
          // TODO: In the AzureEnvironment defined KeyVault (or appSettings.Development.INSECURE.json)  provide the default name of resources within the above ResourceGroup.
          // If not set will be lowercase of ResourceGroupName, after dashes removed.
          "DefaultResourceName": "",
          // TODO: In the AzureEnvironment defined KeyVault (or appSettings.Development.INSECURE.json)  provide the App Insights Client Id/Key.
          "AppInsightsClientIdentifier": "",
          // TODO: In the AzureEnvironment defined KeyVault (or appSettings.Development.INSECURE.json)  provide the StorageAccount resource name used to persist diagnostics messages.
          // Required diagnostics trace messages are output to a storage account:
          "DiagnosticsStorageAccountResourceName": ""
        },
        // -=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=
        "DefaultAzureStorageAccountConfiguration": {
          // TODO: In the AzureEnvironment defined KeyVault (or appSettings.Development.INSECURE.json) enable the service.
          "Enabled": false,
          // Not required for this service.
          "BaseUri": "",
          // TODO:  In the AzureEnvironment defined KeyVault (or appSettings.Development.INSECURE.json)  provide the Redis Server ConnectionString, minus the password.",
          "ClientIdentifier": "",
          // TODO:  In the AzureEnvironment defined KeyVault (or appSettings.Development.INSECURE.json)  provide the Redis Server ConnectionString, minus the password.",
          "ClientSecret": "",
          "MiscConfig": ""
        },
        // -=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=
        "AzureMapsServiceConfiguration": {
          // TODO: In the AzureEnvironment defined KeyVault (or appSettings.Development.INSECURE.json) enable the service.
          "Enabled": false,
          "BaseUri": "",
          // TODO:  In the AzureEnvironment defined KeyVault (or appSettings.Development.INSECURE.json)  provide the Redis Server ConnectionString, minus the password.",
          "ClientIdentifier": "",
          // TODO:  In the AzureEnvironment defined KeyVault (or appSettings.Development.INSECURE.json)  provide the Redis Server ConnectionString, minus the password.",
          "ClientSecret": "",
          "MiscConfig": ""
        },
        // -=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=
        // Configuration for Redis Cache based Distributed Cache Service 
        // required for horizontal scalability
        "DistributedCacheServiceConfiguration": {
          // TODO: In the AzureEnvironment defined KeyVault (or appSettings.Development.INSECURE.json) enable the service.
          "Enabled": false,
          // Not required for this service.
          "BaseUri": "",
          // Not required for this service.
          "ClientIdentifier": "",
          // TODO:  In the AzureEnvironment defined KeyVault (or appSettings.Development.INSECURE.json)  provide the Redis Server ConnectionString, minus the password.",
          "ClientSecret": "TODO: The password to the remote Redis service.",
          // Not required for this service.
          "MiscConfig": ""
        },
        // -=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=
        // Configure for IpStack
        "GeoLocationServiceConfiguration": {
          // TODO: In the AzureEnvironment defined KeyVault (or appSettings.Development.INSECURE.json) enable the service.
          "Enabled": false,
          // Not required for this service.
          "BaseUri": "",
          // Not required for this service.
          "ClientIdentifier": "",
          // TODO:  In the AzureEnvironment defined KeyVault (or appSettings.Development.INSECURE.json)  provide the ipstack.com client key
          "ClientSecret": "",
          // Not required for this service.
          "MiscConfig": ""
        },
        // -=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=
        // Configuration to validate updated media:
        "MediaMalwareDetectionServiceConfiguration": {
          // TODO: In the AzureEnvironment defined KeyVault (or appSettings.Development.INSECURE.json) enable the service.
          "Enabled": false,
          // Not required for this service.
          "BaseUri": "",
          // TODO:  In the AzureEnvironment defined KeyVault (or appSettings.Development.INSECURE.json)  provide the scanii.com client id
          "ClientIdentifier": "",
          // TODO:  In the AzureEnvironment defined KeyVault (or appSettings.Development.INSECURE.json)  provide the scanii.com client secret
          "ClientSecret": "",
          // Not required for this service.
          "MiscConfig": ""
        },
        // -=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=
        // Configuration for SMTP service needed
        // to send email notifications
        // For Examples, using a GMail/Google Apps account, 
        // use "smtp.gmail.com", 587, "support@myorg.com", "avaliduser@myorg.com"
        // And for the ClientSecret, navigate to
        // https://myaccount.google.com/security 
        // then, as per https://support.google.com/accounts/answer/185833, 
        // select Security, and Generate a new app account + password.
        // (If you don't, and you have 2FA, you'll get a 5
        "SmtpServiceConfiguration": {
          // TODO: In the AzureEnvironment defined KeyVault (or appSettings.Development.INSECURE.json) enable the service.
          "Enabled": false,
          // TODO: In the AzureEnvironment defined KeyVault (or appSettings.Development.INSECURE.json) provide the base Uri (eg: smtp.gmail.com)
          "BaseUri": "",
          // TODO: In the AzureEnvironment defined KeyVault (or appSettings.Development.INSECURE.json) provide the port (eg: 587)
          "Port": "",
          // TODO: In the AzureEnvironment defined KeyVault (or appSettings.Development.INSECURE.json) provide eg: support.myapp@myorg.com
          "From": "",
          // TODO: In the AzureEnvironment defined KeyVault (or appSettings.Development.INSECURE.json) provide eg: myaccount@myorg.com
          "ClientIdentifier": "",
          // TODO: In the AzureEnvironment defined KeyVault (or appSettings.Development.INSECURE.json) provide a password
          // As per https://support.google.com/accounts/answer/185833, 
          // Navigate to https://myaccount.google.com/security 
          // To generate a custom App password
          "ClientSecret": "",
          // Not required for this service.
          "MiscConfig": ""
        }
        // -=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=
      }
    }
  }
}
